#!/usr/bin/python3
def best_score(a_dictionary):
    biggest_key = []
    for key in biggest_key.keys():
        if key >= a_dictionary.keys():
            return biggest_key
        else:
            return None
